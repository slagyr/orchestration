# Connection details for the remote Isaac host (e.g. zanebot).
# This file is committed as a template. Copy to .env and edit.
# The real values are used by install.sh and by the agent following
# isaac-beans/test/happy-path.md.
HOST=host.example.com
USER=joe

# Optional: override the target Isaac root (default: ~/.isaac)
# ISAAC_ROOT=~/.isaac

# Optional: local git checkout that should receive
# .github/workflows/ci-failure-hail.yml when running ./isaac-ci/install.sh
# CI_REPO_DIR=/Users/micahmartin/agents/isaac/plan/isaac-hail

# Optional: local convenience values for configuring GitHub repos
# with `gh variable set` / `gh secret set`
# ISAAC_HAIL_URL=https://zanebot.tail66e5f8.ts.net/hail/send
# ISAAC_SERVER_AUTH_TOKEN=replace-me
